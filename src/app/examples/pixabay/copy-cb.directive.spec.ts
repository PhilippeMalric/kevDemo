import { CopyCBDirective } from './copy-cb.directive';

describe('CopyCBDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyCBDirective();
    expect(directive).toBeTruthy();
  });
});
